baseURL = 'https://hanshui.space/'
languageCode = 'zh-cn'
title = '陆寒水的博客'
theme = 'github.com/janraasch/hugo-bearblog'

# 开启 Git 自动追踪修改时间
enableGitInfo = true

[frontmatter]
  lastmod = [":git", "lastmod", "date"]

[params]
  description = "A personal space"
  # 关键：告诉 Bear Theme 引入我们的 custom.css
  custom_head = '<link rel="stylesheet" href="/custom.css">'
